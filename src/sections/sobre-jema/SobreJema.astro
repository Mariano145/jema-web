<section id="sobre-jema" class="relative overflow-hidden bg-text text-background py-16 md:py-20 lg:py-24">
  <!-- Decorative blob bridges at top and bottom (mirror footer blob pattern, inverted for dark theme) -->
  <div class="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true">
    <svg class="absolute -top-24 -left-12 h-80 w-80 text-secondary opacity-20 blur-3xl" viewBox="0 0 200 200">
      <circle cx="100" cy="100" r="80" fill="currentColor" />
    </svg>
    <svg class="absolute -bottom-24 -right-12 h-80 w-80 text-secondary opacity-20 blur-3xl" viewBox="0 0 200 200">
      <circle cx="100" cy="100" r="80" fill="currentColor" />
    </svg>
  </div>

  <div class="relative z-10 mx-auto max-w-3xl px-6 md:px-8">
    <p class="text-sm font-medium uppercase tracking-widest text-secondary">Nosotros</p>
    <h2 class="mt-4 text-3xl font-bold tracking-tight text-background md:text-4xl lg:text-5xl">
      Por qué existimos
    </h2>
    <p class="mt-6 text-lg leading-relaxed text-background/75">
      No somos una agencia que entrega y desaparece. Somos un partner tecnológico que se queda.
    </p>
    <p class="mt-8 text-lg leading-relaxed text-background/80">
      JEMA existe para que empresas que quieren escalar no tengan que sumar equipo para hacerlo.
      Automatizamos lo repetitivo, construimos lo que no existe y operamos lo que desplegamos. La
      diferencia es que después del deploy no nos vamos: nos quedamos operando y mejorando tus
      sistemas en el tiempo.
    </p>
  </div>
</section>
